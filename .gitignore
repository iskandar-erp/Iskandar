# Rust
target/
**/*.rs.bk

# Usar iskandar.example.toml como plantilla
iskandar.toml
# Usar iskandar.waivers.example.toml como plantilla — estado de decisión
# con timestamp, no configuración compartida
iskandar.waivers.toml
*.fdb


# (contiene datos de infraestructura propia y documentación propietaria)
Contexto/
Microsip/

# La landing es su propio repo no va en el core
web/

# Herramientas locales (sesiones de agentes, editores)
.claude/
.vscode/
.idea/

# Variables de entorno locales
.env
.env.*

# Reportes locales de strix (pueden contener detalles de infraestructura)
strix_runs/
